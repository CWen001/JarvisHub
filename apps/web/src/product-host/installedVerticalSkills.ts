// Vertical Packages are native Skills. This small registry only identifies the
// mutually exclusive professional Skills exposed by the shared Product View.
export const installedVerticalSkills = Object.freeze([
  'watch-design-kernel',
  'tablet-design-kernel',
] as const)
